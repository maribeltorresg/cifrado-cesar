

function isValiCard(numberCard){
    var arr = [];
    for (var i = 0; i < number.lentgh; i++){
        var item = arr.pop();
        arr.splice(i,0,item);
    }
    //veremos si la posicione es par

}
var numberCard = parseInt(prompt('Escribe tu numero de tarjeta'));
isValiCard(numberCard);